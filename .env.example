SANITY_STUDIO_PROJECT_ID=""
SANITY_STUDIO_DATASET="production"
SANITY_WRITE_TOKEN=""

# Optional — Cloudflare Web Analytics beacon token.
# Get it in the Cloudflare dashboard: Analytics & Logs → Web Analytics → your
# site → "Manage site" → copy the token from the JS snippet. Leave empty to
# disable (or use Pages' automatic Web Analytics instead — don't do both).
PUBLIC_CF_BEACON_TOKEN=""
